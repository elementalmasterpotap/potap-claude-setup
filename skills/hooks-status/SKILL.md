---
name: hooks-status
description: Аудит системы хуков — какие правила захукированы, какие нет, что добавить. Вызывать для проверки покрытия правил хуками.
allowed-tools: Read, Bash
---

Покажи полный статус системы хуков.

## Шаг 1 — прочитай settings.json

```bash
cat ~/.claude/settings.json | python3 -c "import sys,json; h=json.load(sys.stdin)['hooks']; [print(k,len(v)) for k,v in h.items()]"
```

## Шаг 2 — список скриптов

```bash
ls -la ~/.claude/scripts/*.py
```

## Шаг 3 — таблица хуков

| Хук | Тип | Что проверяет | Действие | Файл существует |
|-----|-----|---------------|----------|----------------|

Заполни по данным из settings.json + ls скриптов.

## Шаг 4 — карта покрытия правил

Пройдись по всем md-правилам и покажи:

| Правило | Файл | Захукировано | Хук |
|---------|------|-------------|-----|

Итог: сколько правил захукировано vs невозможно автоматизировать vs не захукировано.
