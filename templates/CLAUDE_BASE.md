# [НАЗВАНИЕ ПРОЕКТА]
# Sync: ~/.claude/CLAUDE.md → .claude/universal-rules.md | ~/.claude/memory/MEMORY.md → .claude/universal-memory.md

## Проект
[Одна строка — что это, зачем, какую проблему решает]

## Активный файл
[Главный файл которого касается большинство правок — путь]
[Другие важные файлы]

## Заморожено
[Что нельзя трогать и почему — критичные зависимости, проверенные нельзя-переписывать участки]
Откат: [команда отката к предыдущей версии]
Эталон: [команда отката к стабильной]

## Haiku
Анализ / поиск / структура → codebase-explorer (Haiku субагент).
Правки файлов → Sonnet (main context).
[→ Haiku] писать перед каждым вызовом субагента.
Детали → `~/.claude/rules/haiku-economy.md`

## Перед началом работы
`tasks/lessons.md` · `VIBE.md` (если есть маппинг запросов→изменения)

## Правила
`.claude/rules/workflow.md` — деплой, патчноты
`.claude/rules/preferences.md` — цвета, структура, стиль
Правила правил → `~/.claude/CLAUDE.md` + `~/.claude/memory/MEMORY.md`

## Метафоры (заполни под проект)
CLAUDE.md=гримуар · баг=мини-босс · деплой=спелл · рефакторинг=левел-ап
откат=phoenix down

---
# Шаблонный блок — не менять (универсальные правила)

## Алгоритм сессии
```
ИНИТ (один раз):
  communication.md прочитан? НЕТ──► читать (БЛОКЕР)
  MEMORY.md прочитан?        НЕТ──► читать ──► активировать триггеры

ЗАДАЧА ПОЛУЧЕНА:
  3+ шагов или архит. решение? ──► Plan mode
  Параллельный анализ?         ──► Subagents — один subagent, одна задача
  Неоднозначность?             ──► допущение в ответ, не вопрос
  Риск потери работы / крит.?  ──► спросить перед действием

ВЫПОЛНЕНИЕ:
  [ВЫСОКИЙ РИСК]?      ──► озвучить план и допущения явно перед правкой
  Заблокирован?        ──► паттерн в коде ──► docs/ ──► альтернатива
  План пошёл не так?   ──► СТОП ──► перепланировать, не толкать дальше

ПОСЛЕ ЗАДАЧИ:
  поломка / урок?          ──► tasks/lessons.md
  новый паттерн?           ──► MEMORY.md
  визуальное изменение?    ──► docs/PATCHNOTES.md
  готово?                  ──► доказать что работает перед отметкой выполненным
```

## Permissions
`src/` `lib/` — свободно
`docs/` `README` — дополнять, не переписывать
`CI` `infra/` `*.yml` — спрашивать
`logo/` `assets/` — не трогать

## Commits
`type(scope): description` · feat/fix/refactor/docs/style/chore · не амендить опубликованные

## Принципы
Минимальный импакт · root cause не симптом · трогать только нужное · не хакать · не дублировать
Нетривиальная правка → спросить себя "есть более элегантный путь?" перед отправкой
